name: CICD PIPELINE

on:
  push:


jobs:
     code:
         uses: ./.github/workflows/code.yml #calling workflow
     build:
       uses: ./.github/workflows/build.yml
     test:  
        uses: ./.github/workflows/test.yml
     deploy: 
        uses: ./.github/workflows/deploy.yml
     
